{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "watch:sass": "node-sass sass/main.scss public/css/style.css -w",
    "start": "nodemon app.js",
    "build": "esbuild app.js --bundle --platform=node --packages=external --format=esm --outfile=build/app.js"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "dotenv": "^16.1.3",
    "express": "^4.18.2",
    "pug": "^3.0.2"
  },
  "devDependencies": {
    "node-sass": "^9.0.0"
  }
}
